<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FinalProject"
    x:Class="FinalProject.AppShell"
    Shell.FlyoutBehavior="Flyout"
    FlyoutBackgroundColor="#1B1C3A"
    BackgroundColor="#0F1021"
    Title="ZodiacScope">

    <FlyoutItem Title="Home">
        <ShellContent Title="Home" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Enter Zodiac Info">
        <ShellContent Title="Enter Info" Route="DataEntryPage" ContentTemplate="{DataTemplate local:DataEntryPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Saved Readings">
        <ShellContent Title="Saved Readings" Route="DataDisplayPage" ContentTemplate="{DataTemplate local:DataDisplayPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Settings / About">
        <ShellContent Title="Settings" Route="SettingsPage" ContentTemplate="{DataTemplate local:SettingsPage}" />
    </FlyoutItem>
</Shell>
